<div class="wrap" style="padding:100px 22px;text-align:center">
  <h1 style="font-family:var(--serif);color:var(--wine-deep)">404</h1>
  <p style="color:var(--ink-soft);margin:12px 0 22px">{{ 'general.404.title' | t }}</p>
  <a href="{{ routes.root_url }}" class="btn">{{ 'general.404.link' | t }}</a>
</div>
