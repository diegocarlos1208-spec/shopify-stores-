<div class="wrap" style="padding:64px 22px;max-width:760px;margin:0 auto">
  <h1 style="font-family:var(--serif);color:var(--wine-deep);margin-bottom:18px">{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</div>
